=== ITERATION 01 SUMMARY ===

Goal:
Create reliable deterministic infrastructure for requirement parsing
and acceptance-criterion generation.

Changes:
- Added structured requirement parsing.
- Added Specification and Requirement models.
- Added deterministic requirement decomposition.
- Added parser and decomposer unit tests.
- Added pytest configuration for the project.

Evidence:
- Parsed all 6 numbered requirements from Case 01.
- Parser tests passed.
- Decomposer tests passed.

Limitation discovered:
The deterministic decomposer was syntactic rather than semantic.

Example:
"Not begin or end with an underscore."

was initially treated as one acceptance criterion even though it contains
two independently testable behaviors.

Learning:
Deterministic parsing is useful for traceability, but semantic reasoning
is needed to produce truly atomic acceptance criteria.

Decision:
Keep deterministic parsing as infrastructure and introduce an LLM-based
semantic decomposition agent in the next iteration.